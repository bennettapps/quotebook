<%= simple_form_for(@homie) do |form| %>
  <% if homie.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(homie.errors.count, "error") %> prohibited this homie from being saved:</h2>

      <ul>
        <% homie.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
